# jenkins_scripts_with_tests
poc for tests for jenkins groovy scripts

## Run test locally
- make sure that `npm` and [gradle](https://gradle.org/install/) are installed locally
- go to project
- run `npm run test:groovy`
